import React from 'react';

export default (props) => {
    return (
        <div className="align-center loading-container">
             <i className="fas fa-spinner fa-spin"></i>
        </div>
    );
}